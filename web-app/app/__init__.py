"""web-app: the area-manager dashboard for Ichimaru calibrated sales.

A FastAPI + Jinja2 + HTMX application that reads the pipeline's DATA/ outputs and
proxies chat to agent-service (the Gemma model host). See README.md.
"""
